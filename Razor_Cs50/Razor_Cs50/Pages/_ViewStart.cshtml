@*
    _ViewStart.cshtml
    trước khi đi vào page nó sẽ đi qua đây để nạp dữ liệu trước,
    tất cả các page ở trong pages (bao gồm cả thư mục con) sẽ đi qua đây
    nếu muốn thêm viewstart cho các areas thì ta chỉ việc thêm vao folder areas viewstart là tất cả areas sẽ nhận được
*@
@{
    Layout = "_MyLayout"; //Thiết lập layout chung cho toàn page

     //Thiết lập dữ liệu chung (Phần này đang đoán là vậy chưa bt có dùng được hay không)
    string CommonData = "abc";
    string Name = "minh";
    //.... bất kể thứ gì bạn muốn dùng chung
}
